# yaml-language-server: $schema=https://raw.githubusercontent.com/jesseduffield/lazygit/master/schema/config.json
os:
  editPreset: "nvim"
gui:
  expandFocusedSidePanel: true
git:
  autoFetch: false
  branchPrefix: "u/lrabello/"
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never
